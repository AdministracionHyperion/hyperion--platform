# Voice Provider Events

Contracts for safe mock provider event ingestion. This module only accepts mock events, synthetic
provider references, sanitized payloads, synthetic signatures, and in-memory replay protection.

No real provider webhook, provider SDK, network egress, raw transcript, raw audio, phone number,
secret, or persistent raw payload is implemented here.
