# Core Operations Dashboard

Read-only operational dashboard contracts for Hyperion. The module builds sanitized read models for
API and web surfaces. It does not dispatch jobs, call providers, read secrets or enable real calls.
